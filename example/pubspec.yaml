name: anchored_popover_example
publish_to: none

environment:
  sdk: ^3.13.0

dependencies:
  anchored_popover:
    path: ../
  flutter:
    sdk: flutter
  material_ui: ^1.0.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
