name: anchored_popover_example
publish_to: none

environment:
  # The oldest SDK the package itself supports: an example that needs a
  # newer one is not an example of what the package promises.
  sdk: ^3.12.0

dependencies:
  anchored_popover:
    path: ../
  flutter:
    sdk: flutter
  material_ui: ^1.1.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
